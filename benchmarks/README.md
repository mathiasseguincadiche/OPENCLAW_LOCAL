# Benchmarks

Les résultats bruts sont écrits dans `benchmarks/results/` et ignorés par Git.

Le script initial mesure une **latence murale simple** et la réussite de la commande. Il ne prétend pas mesurer précisément les tokens/s. Une campagne publiée doit utiliser le protocole décrit dans `docs/BENCHMARK.md`.
